function setup(){
    createCanvas(600, 600);
}

//var x = 0;

// Explore the reference section: p5js.org/reference
function draw(){
    background("#bae");
    ellipse(mouseX, mouseY, 50);
    // x = x + 1;
    //x++;

}